<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.mobile">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        android:usesCleartextTraffic="true">
        <activity android:name=".Views.activities.CreateQuestionActivity" />
        <activity android:name=".Views.activities.CreateConferenceActivity" />
        <activity android:name=".Views.activities.ConferenceActivity" />
        <activity android:name=".Views.activities.LandingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Views.activities.ConferenceListActivity" />
        <activity android:name=".Views.activities.LoginActivity" />
        <activity android:name=".Views.activities.SignupActivity" />
        <activity android:name=".Views.activities.ManageSurveyActivity"></activity>
    </application>

</manifest>